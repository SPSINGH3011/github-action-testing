name: Manual Pipeline


# on:
#   workflow_dispatch:
on:
  push:
    branches:
      - main


jobs:

  build:

    # runs-on: ubuntu-latest
      runs-on: self-hosted

      steps:

      - name: Hello friends
        run: echo "Manual run"
